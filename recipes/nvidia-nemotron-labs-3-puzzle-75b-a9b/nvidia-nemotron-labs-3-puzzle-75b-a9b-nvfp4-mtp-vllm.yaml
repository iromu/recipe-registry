recipe_version: "2"
model: nvidia/NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B-NVFP4
runtime: vllm
max_nodes: 1
container: ghcr.io/spark-arena/dgx-vllm-eugr-nightly:latest

metadata:
  description: nvidia/NVIDIA-Nemotron-Labs-3-Puzzle-75B-A9B-NVFP4
  num_layers: 88

defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.8 # 4.3x (vs max_model_len=262,144)
  max_model_len: 262144
  max_num_batched_tokens: 16384
  max_num_seqs: 8
  kv_cache_dtype: fp8
  tool_call_parser: qwen3_coder
  reasoning_parser: nemotron_v3
  speculative_config: '{"method":"mtp","num_speculative_tokens":3}'

command: |
  vllm serve {model} \
    --served-model-name nemotron-3-puzzle {model} \
    --enable-expert-parallel \
    --async-scheduling \
    --trust-remote-code \
    --mamba-backend flashinfer \
    --mamba_ssm_cache_dtype float16 \
    --enable-mamba-cache-stochastic-rounding \
    --mamba-cache-philox-rounds 5 \
    --speculative-config '{speculative_config}' \
    --kv-cache-dtype {kv_cache_dtype} \
    --max-model-len {max_model_len} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --max-num-seqs {max_num_seqs} \
    --host {host} \
    --port {port} \
    --trust-remote-code \
    --enable-auto-tool-choice \
    --tool-call-parser {tool_call_parser} \
    --reasoning-parser {reasoning_parser} \
    --enable-prefix-caching \
    -tp {tensor_parallel} \
    --gpu-memory-utilization {gpu_memory_utilization}
